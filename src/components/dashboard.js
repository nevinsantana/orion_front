import React from "react";

const Dashboard = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
        fontSize: "2rem",
      }}
    >
      Bienvenido al Dashboard
    </div>
  );
};

export default Dashboard;
